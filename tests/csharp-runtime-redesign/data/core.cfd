text: UiText { welcome: "Hello", count: 17 }

  first: Item {
      stats: Stats { value: 2 },
      next: &second,
      calculate: fn(input: int) -> int { &second.calculate(input) + stats.value },
  }
  self: Item {
      stats: Stats { value: 8 },
      next: &self,
      calculate: fn(input: int) -> int { input + stats.value },
  }
