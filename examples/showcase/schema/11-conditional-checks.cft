# Feature: conditional validation with if.

table ConditionalRule {
  enabled: bool;
  threshold: int;

  check {
    if self.enabled {
      Coflow::Check::require(self.threshold > 0, "enabled rules need a positive threshold");
    }
  }
}
