# Feature: inheritance and polymorphic inline objects.

abstract data Effect {
  label: string;
}

data DamageEffect : Effect {
  amount: int;
}

data HealEffect : Effect {
  amount: int;
}

table EffectBundle {
  primary: Effect;
  additional: [Effect] = [];
}
