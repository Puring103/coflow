# Feature: all, any, and none collection quantifiers.

table QuantifierRule {
  numbers: [int];

  check {
    all value in numbers {
      value >= 0;
    }
    any value in numbers {
      value == 0;
    }
    none value in numbers {
      value < 0;
    }
  }
}
