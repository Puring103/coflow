# Feature: record validation with require.

table RangeRule {
  minimum: int;
  maximum: int;

  check {
    Coflow::Check::require(self.minimum <= self.maximum, "minimum must not exceed maximum");
  }
}
