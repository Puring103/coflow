// Unicode identifiers are valid CFT names.

enum 示例品质 {
  普通,
  稀有,
}

type 示例物品 {
  @id
  编号: string;

  名称: string;
  品质: 示例品质 = 示例品质.普通;

  check {
    编号 != "";
    名称 != "";
    品质 >= 示例品质.普通;
  }
}
