[package]
name = "coflow-extension-api"
version = "0.7.3"
edition = "2021"
description = "Versioned extension manifest protocol for Coflow editors."
license = "Apache-2.0"
publish = false

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
