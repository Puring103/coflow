// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { EditorRecordGroup } from "./EditorRecordGroup";
import type { EditorWorkspaceState } from "./EditorWorkspaceState";
import type { ViewConfig } from "./ViewConfig";

export type EditorProjectSettings = {
/**
 * 按图视图身份保存节点坐标，独立于数据版本，重新打开项目时恢复。
 */
graph_positions: { [key in string]?: { [key in string]?: [number, number] } },
/**
 * 每个记录类型唯一的缩略名字符串字段，跨文件生效。
 */
short_name_fields: { [key in string]?: string },
/**
 * 按文件和类型保存视图标签顺序，包含内置视图和自定义视图的 ID。
 */
view_order: { [key in string]?: { [key in string]?: Array<string> } },
/**
 * Custom views keyed by (filePath, actualType). Default record/table
 * views are implicit and never stored here.
 */
views: { [key in string]?: { [key in string]?: Array<ViewConfig> } },
/**
 * Column widths for the implicit default table view, keyed by
 * (filePath, actualType, columnName). Custom table views carry their
 * own widths inside their [`ViewConfig`].
 */
default_table_column_widths: { [key in string]?: { [key in string]?: { [key in string]?: number } } }, record_groups: { [key in string]?: { [key in string]?: Array<EditorRecordGroup> } }, workspace: EditorWorkspaceState, };
