# Diagnostics

Diagnostics carry a stable code, stage, severity, message and source span. Syntax diagnostics point into CFD/CFT text; project diagnostics point into `coflow.yaml`; semantic diagnostics carry record identity and field path. Human and JSON renderers consume the same canonical fields.
