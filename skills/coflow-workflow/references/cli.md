# CLI

```powershell
coflow init <dir>
coflow cft check <project>
coflow check <project>
coflow codegen <project>
coflow build <project>
coflow schema inspect <project>
coflow lsp <project>
```

There is no `data` or `export` command. Codegen writes only source files declared by `codegen`.
