# 提案：可选访问与空值赋值

状态：部分并入核心。

核心版本已经支持：

1. 可选字段访问`?.`
2. 空值合并赋值`??=`

本提案继续保留尚未进入核心的空值相关语法和语义问题。

## 候选能力

```coflow
var score = scores?["alice"] ?? 0
```

## 待解决问题

1. 是否需要可选索引访问`?[]`。
2. `??=`左侧缺失字段时是否创建字段。
3. 只读配置对象上使用`??=`如何诊断。
