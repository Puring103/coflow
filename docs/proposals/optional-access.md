# 提案：可选访问与空值赋值

核心版本只有缺失即`null`和`??`。

本提案讨论额外空值相关语法。

## 候选能力

```coflow
var name = player?.weapon?.name
var score = scores?["alice"] ?? 0
name ??= "unknown"
```

## 待解决问题

1. 既然缺失读取已经是`null`，`?.`是否仍有必要。
2. `?.`是否只避免错误，还是也表达静态诊断抑制。
3. `??=`左侧缺失字段时是否创建字段。
4. 只读配置对象上使用`??=`如何诊断。

