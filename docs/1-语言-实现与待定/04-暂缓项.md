# 暂缓项

> 状态：首版范围
>
> 范围：尚不进入首版实现的功能。

## 1. 区间与 range-for

首版 for 遍历数组和字典，索引循环使用 while。
区间运算符 ..、..= 及相应遍历语法单独引入。

## 2. match

首版使用 if、is TypeName 和 is Some(name)。
match 的模式语法和穷尽检查单独定义。

## 3. 增量 check

当前检查每次实际执行所选 check，不复用结果，不维护检查依赖图。
解析和函数编译的复用可以独立进行。
增量 check 后续单独评估，不作为当前架构或性能验收的前置要求。

## 4. 通用内建方法

集合高阶方法与查找、数值聚合、数字解析与转换、正则及字符串等通用内建方法暂不实现。
已确定的签名和行为保留在语言规范中，作为后续实现依据；相关验收案例不作为首版完成条件。
查找返回结构及数字解析的剩余边界随这些方法恢复实现时细化。
