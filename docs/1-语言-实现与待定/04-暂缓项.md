# 暂缓项

> 状态：首版范围
>
> 范围：尚不进入首版实现的功能。

## 1. 区间与 range-for

首版 for 遍历数组和字典，索引循环使用 while。
区间运算符 ..、..= 及相应遍历语法单独引入。

## 2. Host 服务声明与绑定

Host 服务的 @Host 声明语法和具体绑定协议暂缓实现。
首版普通函数字段使用 CFT 默认实现和 CFD 覆盖。

语言允许 check 调用任何函数，未来 Host 接入也遵循这一规则。
正式接入时补齐声明、绑定生命周期、异常映射和同步重入协议，不新增 check 调用权限分类。

## 3. match

首版使用 if、is TypeName 和 is Some(name)。
match 的模式语法和穷尽检查单独定义。

## 4. 增量 check

当前检查每次实际执行所选 check，不复用结果，不维护检查依赖图。
解析和函数编译的复用可以独立进行。
增量 check 后续单独评估，不作为当前架构或性能验收的前置要求。
