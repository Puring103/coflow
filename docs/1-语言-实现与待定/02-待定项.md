# 待定项

> 状态：接口细节待定
>
> 范围：不改变已确定语言方向，但仍需命名、细化或性能验证的接口与实现参数。

## 1. 维度遍历内建

遍历能力返回全部静态变体的名称和有效值，基础值单独访问。
内建函数的名称、参数写法和返回结构尚未命名。

## 2. 内建方法清单

高阶方法的完整适用类型、回调签名，以及空集合、NaN 和正则方言等边界需要逐项列出。
find 的保留范围和可选元素返回规则待定。

## 3. Host 接口

Host 声明语法、绑定接口、异常映射和同步重入的具体协议见暂缓项。
check 可调用 Host 的语言原则不依赖具体接口命名。

## 4. 字节码编码参数

寄存器式虚拟机始终解释执行，不引入 JIT。
优先评估 32 位常用指令加扩展编码，与 64 位定长指令比较总体积、执行指令数和耗时。
寄存器编号宽度与上限、Value 大小、指令位布局及调用信息编码尚未定稿。
评估范围见[寄存器与字节码](../2-架构/04-寄存器与字节码.md)。
