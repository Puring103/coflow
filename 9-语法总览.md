# coflow语法总览

本文档用于快速展示coflow当前语法草案

# 模块

```coflow
import common
import weapons
```

一个文件是一个模块
顶层声明默认公开
使用local声明文件内私有成员

```coflow
local var internal_scale = 2

local fn helper() -> int {
  return 10
}
```

# 类型与配置

```coflow
enum Rarity {
  common
  rare
  epic
}

class Weapon {
  id: string
  name: string
  rarity: Rarity
  damage: int
  cooldown: float = 1.0

  fn validate(self) -> null | string | [string] {
    if self.damage <= 0 {
      return "damage must be greater than 0"
    }

    return null
  }
}
```

顶层`name = value`是配置
顶层`name: Type = value`是带类型标注的配置
配置必须在编译期确定

```coflow
base_sword: Weapon = {
  id: "base_sword",
  name: "Base Sword",
  rarity: Rarity.common,
  damage: 10,
}

strong_sword: Weapon = {
  ...base_sword,
  id: "strong_sword",
  name: "Strong " + base_sword.name,
  damage: base_sword.damage + 5,
}
```

# 变量与类型标注

```coflow
var hp: int = 100
var name = "hero"
var target: Entity | null = null
var id: int | string = "sword"
var values: [any] = [1, "hello", true]
var scores: [string: int] = {
  "alice": 10,
  "bob": 20,
}

var [x, y] = point
var [first, ...rest] = items
var { id, damage } = weapon
var { x: px, y: py } = player.position
```

# 函数

```coflow
fn add(a: int, b: int) -> int {
  return a + b
}

fn print_value(value) {
  print(value)
}

var add_one = fn(x: int) -> int {
  return x + 1
}
```

支持命名参数

```coflow
spawn(id: "slime", x: 10, y: 5)
```

# 控制流

```coflow
if hp > 0 and not dead {
  update()
} else {
  respawn()
}

var state = if hp <= 0 {
  "dead"
} else {
  "alive"
}

var label = match state {
  "idle" => "waiting",
  "run" => "moving",
  "dead" => "finished",
  _ => "unknown",
}

var kind = match value {
  is int => "number",
  is string => "text",
  _ => "unknown",
}

while running {
  update()
}

for item in items {
  use(item)
}

for i in 0..items.length {
  print(items[i])
}

for i in 1..=10 {
  print(i)
}

for key, value in scores {
  print(key, value)
}

for { id, damage } in weapons {
  print(id, damage)
}
```

# 表达式

```coflow
var weapon_name = player?.weapon?.name ?? "unknown"
weapon_name ??= "fallback"

var visible_items = items[0..10]
var first_three = items[..3]
var remaining = items[3..]

var all_items = [
  ...base_items,
  sword,
  shield,
]

if item in inventory {
  use(item)
}

if value is string {
  print(value.length)
}

if value is not string {
  return "value must be string"
}
```

范围表达式：

```coflow
0..10   // 左闭右开
1..=10  // 闭区间
```

# class方法

```coflow
class Player {
  hp: int

  fn damage(self, amount: int) {
    self.hp -= amount
  }
}
```

方法使用显式self参数
第一版不支持继承

# 协程

```coflow
fn wait_click() {
  yield wait.event("click")
}

co fn dialog() {
  dialog.show("hello")
  wait_click()
  dialog.show("world")
}

var c = dialog()
c.resume()
```

co fn创建协程上下文
yield可以暂停整个协程调用栈
yield另一个协程时自动委托执行

```coflow
co fn child() {
  yield 1
  yield 2
}

co fn parent() {
  yield child()
  yield 3
}
```

# 暂不支持
1. 字面量联合
2. T?可空语法糖
3. 三元表达式
4. 管道符
5. 位运算
6. 闭包捕获
7. lua式多返回值
8. class继承
9. async/await
10. pub和export
