# 最佳工作流

本页会整理 Coflow 在团队中的推荐使用方式。

## 计划内容

- 先用 CFT 固定数据结构和业务规则。
- 按数据形态选择 Excel、CSV 或 CFD。
- 提交前运行 `coflow check`。
- 构建阶段统一运行 `coflow build`。
- 程序消费生成的 JSON、MessagePack 或 C# 代码。
- 策划和 AI agent 根据诊断持续修复数据。
