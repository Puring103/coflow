# 示例

本页会说明如何运行仓库中的示例项目，并完成第一次 `check` 与 `build`。

## 计划内容

- 运行 `examples/rpg`。
- 理解示例目录结构。
- `coflow check` 与 `coflow build` 的区别。
- 生成目录说明。
- 常见失败与下一步。

```powershell
coflow check examples/rpg
coflow build examples/rpg
```

更多示例说明见 [RPG 示例](../../examples/rpg.md)。
