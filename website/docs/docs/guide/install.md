# 安装

本页会说明如何安装 Coflow CLI，并确认本机可以运行 `coflow` 命令。

## 计划内容

- 从源码安装本地 CLI。
- 确认 `cargo` 和 Rust toolchain。
- 运行 `coflow --help`。
- 常见安装失败。

```powershell
cargo install --path . --force
coflow --help
```
