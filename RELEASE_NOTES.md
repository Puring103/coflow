# Coflow 0.11.0

## 重点更新

### 图谱编辑

- 图谱中的引用关系现在可以直接拖线创建、修改和删除，支持普通引用、集合引用，以及从连线起点、终点或线体重新连接。
- 拖线到空白处可以新建记录并自动建立引用；创建记录与写入引用合并为一步撤销操作。
- 新增图谱框选与多选拖动，选中的节点和连线可以成组移动或批量删除。
- 节点位置、缩放和视口会在编辑及视图切换后保持稳定，手动移动节点和重新布局均支持撤销与重做。
- 优化关系端口和连线的显示与命中范围，并让集合字段中的连线准确对齐到对应元素。

### 编辑器工作流

- 内置视图与自定义视图标签支持拖动排序，并按文件和记录类型保存顺序。
- 每种记录类型可以指定一个字符串字段作为缩略名；记录、引用选项、图谱节点和 Inspector 会使用更易识别的 `缩略名(ID)` 展示。
- 表格、记录和图谱视图统一使用记录右键菜单，可按当前上下文跳转、分组、重命名、插入或删除记录。
- 改进图谱选择状态、关系方向和布局更新，表格编辑与范围选择在数据刷新后保持稳定。

## 性能

### 项目加载

- 修复记录源码跨度换算中的二次复杂度，大项目加载速度大幅提升（万条级项目中约从十几秒降到一秒）。
- 会话构建时合并对全部记录的重复扫描，图谱可用字段改为单次遍历收集。
- 模型构建减少每条记录的重复克隆。

### 源码编辑器

- 打开源码立即显示词法高亮，不再等待语言服务返回。
- 修复源码语义着色与文档大纲的二次复杂度，打开和编辑大文件不再卡顿。
- 初始语义着色同步不再等待编辑去抖延迟。
- 缓存 schema 字段形状与引用目标，减少逐单元格重复计算。

### 视图切换

- 缓存表格列准备（列类型、显示名、列宽），来回切文件近乎即时。
- 表格列宽改用后端已下发的行摘要，行级诊断预建索引，列类型只扫描一次。
- 图谱能力判定按文件与数据版本缓存。
- 未保存草稿的项目级校验复用已加载会话缓存，不再为每次编辑整项目从磁盘重新加载。
- 插件记录字段改为一次批量克隆。
